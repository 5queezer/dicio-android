syntax = "proto3";

option java_package = "org.stypox.dicio.settings.datastore";
option java_multiple_files = true;

enum FallbackMode {
  FALLBACK_MODE_UNSET = 0;
  FALLBACK_MODE_TEXT = 1;
  FALLBACK_MODE_LLM = 2;
}
